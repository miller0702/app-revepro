import { spacing } from './tokens';

/** Margen horizontal estándar de pantallas, headers y listas de cards. */
export const SCREEN_PADDING_X = spacing.md;

/** Separación entre iconos del header (menú, título, buscar). */
export const HEADER_ROW_GAP = spacing.sm;

/** Ancho reservado para botones de acción en el header (menú / buscar). */
export const HEADER_ACTION_SIZE = 44;
